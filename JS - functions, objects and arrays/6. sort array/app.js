// array sorting

let participants = ['Emina', 'Naida', 'Samedin', 'Mirza', 'Abdurahman', 'Imran', 'Amer', 'Alan']

let sortedParticipants = participants.sort();
let reversedSorted = participants.reverse();

console.log(reversedSorted)