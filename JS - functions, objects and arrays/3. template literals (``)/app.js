// TEMPLATE LITERALS = are written: ` `
// Koirste se umjeto '' or ""
// They allow entering variables or e.g
// some mathematical expressions inside the string

function addition(x, y) {
    return x + y;
}

// let result = addition(1, 1);

// console.log(`The result is:`, result)
// console.log(`The result is: ${result} ${1 + 10}`)