// FUNCTIONS = defined once, used multiple times
// to "call" the function, write its name (functionPr())
let firstNumber = 10;

function addition(x, y) {
    console.log(firstNumber)
}
if (x < z) {

}

addition(10, 15);